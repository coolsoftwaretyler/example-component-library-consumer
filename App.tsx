import React from 'react';
import {SafeAreaView} from 'react-native';
import {Counter} from './Counter/Counter';

const App = () => {
  return (
    <SafeAreaView>
      <Counter />
    </SafeAreaView>
  );
};

export default App;
